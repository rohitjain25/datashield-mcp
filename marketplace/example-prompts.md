# Example Prompts for DataShield MCP

- "Inspect this CSV file and tell me its size, row count, and column names."
- "Profile the dataset to see missing values and data types per column."
- "Detect any duplicate rows based on the email column."
- "Scan for personally identifiable information and show masked examples."
- "Check for API keys or secrets in this configuration file."
- "Normalize column names to snake_case and trim whitespace."
- "Validate that the `age` column is between 0 and 120."
- "Clean the dataset: trim whitespace, standardize empty strings, drop exact duplicates."
- "Convert this Excel file to CSV for easier processing."
- "Generate a Markdown report of the data quality with a DataShield Quality Score."
- "Preview what changes would happen if I hash the PII columns."
- "Create a sanitized copy of the file with a timestamp in the filename."